# 行動端 / Safari 限制邏輯 — 人工測試清單

我這邊的開發環境沒有真實手機、平板或 Safari 瀏覽器可以實際操作測試，
以下這份清單是根據程式碼邏輯（`device-profiler.js`）逐條推導出「應該
會發生什麼」，麻煩你實際操作驗證，有任何一項不符合預期都請回報。

## 工具準備

**沒有真實裝置的話，Chrome/Edge 開發工具的裝置模擬可以做第一輪測試**
（按 F12 → 左上角手機/平板圖示，或 `Ctrl+Shift+M`）：
- 可以正確模擬：螢幕寬度、UserAgent 字串、觸控點數量（`maxTouchPoints`）
- **模擬的已知限制**：`navigator.deviceMemory`、實際的記憶體/效能表現、
  某些瀏覽器專屬 API 行為，開發工具模擬不出來，這些項目務必找真實
  裝置測試才準確。

---

## 測試項目一：行動端（手機）判定與封鎖

**用手機（iPhone 或 Android）用瀏覽器實際打開頁面測試：**

| 檢查項 | 預期結果 |
|---|---|
| 頁首橫幅 | 應顯示「偵測到行動裝置：影音轉檔功能已停用...」的提示 |
| 「影音轉檔」分頁按鈕 | 應該是灰色、無法點擊狀態；點擊會跳出「此裝置不支援影音轉檔」的 Modal |
| 「文件轉檔」分頁 | 應該顯示「行動裝置僅支援 5MB 以內的小型文件轉檔」的提示文字 |
| 上傳一份 > 5MB 的 Word/PDF | 應該被阻擋，跳出對應錯誤訊息，無法開始轉檔 |
| 上傳一份 < 5MB 的檔案 | 應該可以正常轉檔 |
| 「圖片轉檔」分頁 | 應該完全不受限制，可以正常使用 |

**用開發工具模擬手機再測一次（作為第一輪快篩）：**
選擇任一款 iPhone 或 Android 機型 → 重新整理頁面 → 重複上表流程。

---

## 測試項目二：平板判定

**用真實 iPad 或 Android 平板測試（若無真實平板，開發工具選平板機型模擬）：**

同上表，平板應該套用跟手機完全一樣的限制（`isMobileOrTablet` 判定
不分手機/平板）。

**⚠️ 特別注意的邊界情況**：部分 Windows 觸控筆電或桌上型觸控螢幕也會
被開發工具的裝置模擬誤判，但這種情況現實中應該正確判定成「桌面版」
（因為螢幕寬度通常 ≥ 1024px，且 UserAgent 不含 Android/iPhone/iPad/
Mobile 關鍵字）。若你剛好有觸控螢幕的 Windows 電腦，麻煩額外測一次：
**應該要能正常使用影音轉檔**，不應該被誤判為行動裝置。

---

## 測試項目三：Safari（Mac 桌面版）150MB 硬性限制

**需要一台 Mac 用 Safari 瀏覽器測試：**

| 檢查項 | 預期結果 |
|---|---|
| 頁首橫幅 | 應顯示「由於 Safari 瀏覽器底層限制，大檔案轉檔請改用 Chrome 或 Edge 瀏覽器（上限 150MB）」 |
| 「影音轉檔」分頁的提示文字 | 應該顯示 Safari 限制的說明文字 |
| 上傳一份 > 150MB 的影片 | 應該被阻擋，跳出「Safari 硬性限制」的 Modal，無法開始轉檔 |
| 上傳一份 < 150MB 的影片 | 應該可以正常轉檔（前提是 FFmpeg.wasm 在 Safari 上運作正常，這部分本身也需要另外驗證，見下方注意事項） |

**⚠️ 重要提醒**：我們先前在 Edge 上花了很多輪才排查出 FFmpeg.wasm
0.11.x 在特定環境下才能正常運作；Safari 的 WebAssembly / Worker
實作跟 Chromium 系瀏覽器有不少差異，**即使檔案大小在 150MB 限制內，
FFmpeg.wasm 本身在 Safari 上能不能正常運作是完全獨立的另一個問題，
務必實際測試一次影片轉檔，不要只驗證「檔案大小限制」這一件事**。
如果在 Safari 上 FFmpeg 也卡住/失敗，麻煩比照先前的排查方式（獨立
測試頁面、Console 完整訊息）回報給我。

---

## 測試項目四：Chromium（Chrome/Edge）300MB 柔性提醒

**桌面版 Chrome 或 Edge（一般測試環境）：**

| 檢查項 | 預期結果 |
|---|---|
| 上傳一份 < 300MB 的影片 | 直接可以轉檔，不會有任何提醒 |
| 上傳一份 > 300MB 的影片 | 顯示柔性警告文字，但「開始轉檔」按鈕**仍然可以點擊**，點下去會照常開始轉檔（不是硬性阻擋） |

---

## 測試項目五：跨裝置的記憶體風險評估

`navigator.deviceMemory` 只有 Chromium 系瀏覽器支援，Safari 完全不
支援。

| 瀏覽器 | 預期行為 |
|---|---|
| Chrome/Edge | 選擇大檔案時，若「裝置記憶體 × 0.3 < 檔案大小」，選檔當下應該出現非阻擋橫幅提示；按下開始轉檔時，若風險仍在，應該跳出需要確認的 Modal |
| Safari | 因為讀不到真實記憶體數值，會 fallback 用 4GB 當估計值計算，橫幅提示文字應該會註明「為估計值」字樣 |

---

## 回報方式

測完麻煩告訴我：
1. 哪些項目符合預期 ✅
2. 哪些項目不符合預期 ❌（附上瀏覽器/裝置型號、Console 錯誤訊息、
   截圖）
3. 特別是 Safari 的 FFmpeg.wasm 實測結果，這是目前完全沒有驗證過、
   風險最高的一塊
